{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "java (buildArtifact)",
			"mainClass": "Picture",
			"targetPath": "${workspaceFolder}/${workspaceFolderBasename}.jar",
			"elements": [
				"${compileOutput}",
				"${dependencies}",
				"${workspaceFolder}"
			],
			"problemMatcher": [],
			"label": "java (build WinterScene.jar)"
		}
	]
}